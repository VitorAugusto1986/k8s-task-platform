apiVersion: v1
kind: Service
metadata:
  name: task-platform
  namespace: task-platform
spec:
  selector:
    app: task-platform
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP

